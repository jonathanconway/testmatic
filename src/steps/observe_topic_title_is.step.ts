import { createStep } from "../framework";

export const observe_topic_title_is = createStep(
  "observe_topic_title_is",
  undefined,
  async () => {
    console.log("observe_topic_title_is");
  }
);
